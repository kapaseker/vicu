package com.rockbyte.vicu.ui.theme

import androidx.compose.ui.unit.dp

/**
 * DESIGN.md spacing 刻度。container-max（1440px）为 web 视口概念，不适用于移动端，未纳入。
 */
object VicuSpacing {
    val unit = 8.dp
    val gutter = 24.dp
    val marginX = 40.dp
    val headerHeight = 72.dp

    /** DESIGN.md Components：卡片内边距下限 32px。 */
    val cardPadding = 32.dp
}
